"""提示词模板"""

# 数学推理Agent系统提示词
SYSTEM_PROMPT_BASE = """你是一个数学推理助手。请逐步解决下面的数学题。

对于每一步，请使用以下格式：
[步骤 N] [动作: 推理|计算|验证|修正]
内容...

最后给出答案：答案: <数字>

重要规则：
- 必须逐步推理
- 每一步都要检查前面的计算
- 最终答案必须是数字"""

# 通用反馈提示词（组B用）
GENERIC_FEEDBACK_PROMPT = """## 改进建议

请在做下一组题时注意以下几点：
1. 仔细检查每一步的计算
2. 不要跳过验证步骤
3. 如果发现错误，及时修正
4. 确保最终答案与推理过程一致
5. 对于复杂问题，尝试用不同方法验证结果"""

# 自我认知报告生成提示词
SELF_REPORT_PROMPT = """基于对你在上一轮表现的因果结构分析，生成一份自我认知报告。

## CSSR分析结果
- 统计复杂度 C_μ: {C_mu:.4f} bits（你的行为模式的内部多样性）
- 超熵 E: {E:.4f} bits（你的行为结构一致性）
- 熵率 h_μ: {h_mu:.4f} bits（你每步推理的不确定性）
- 预测信息 χ: {chi:.4f} bits（内部状态对下一步的预测能力）
- 因果状态数: {n_states}

## 因果状态描述
{state_descriptions}

## 关键发现
{key_findings}

## 任务
请基于以上分析，以第一人称视角写一份简洁的自我认知报告（不超过300字），包括：
1. 我目前的行为模式是什么样的（基于因果状态结构）
2. 我在哪些情况下容易出错
3. 我应该如何改进"""

# 行为编码提示词
BEHAVIOR_ENCODE_PROMPT = """分析以下数学推理步骤，将其编码为行为符号序列。

每个步骤编码为: 动作类型_结果质量

动作类型: INFER(推理推导), CALC(算术计算), VERIFY(验证检查), CORRECT(自我修正)
结果质量: CORRECT(正确), PARTIAL(部分正确), INCORRECT(错误), SKIPPED(跳过)

输入推理步骤:
{steps_text}

请输出编码结果，格式如下（每行一个符号，不要其他内容）：
INFER_CORRECT
CALC_CORRECT
VERIFY_SKIPPED
..."""
